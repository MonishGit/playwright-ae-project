# Dependencies — regenerated by npm ci 
node_modules/ 

# Playwright test output — generated each run test-results
/ playwright-report/
playwright-report/
results.xml

# Auth state — contains session cookies, never commit 
auth.json 

# TypeScript compiled output 
dist/ 

# Debug screenshots we created 
debug-signup.png 
*.png 

# Environment variables — never commit secrets 
.env 
.env.local
.env.production

# OS files 
.DS_Store 
.Thumbs.db
.DS_Store?
._*

# VS Code settings (optional — some teams commit this) 
.vscode/

# Playwright browsers — downloaded by npx playwright install
# No need to commit — CI downloads them fresh each run
/ms-playwright/
